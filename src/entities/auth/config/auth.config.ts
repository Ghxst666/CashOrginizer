export const AUTH_ENDPOINTS = {
    REGISTER: 'auth/register',
    RESET_PASSWORD: 'auth/reset-password',
    RECOVERY_CODES_REISSUE: 'auth/recovery-codes/reissue',
    LOGOUT: 'auth/logout'
} as const
